# AlgoDect

AlgoDect is a command line math based AI detector that uses text splitting, character count, word count, and punctuation count to determine if the provided text has a cadence that is either human or AI generated.

# Installation process
Clone the repo and install

```language
$ git clone https://github.com/gamefox229/AlgoDect
$ cd algodect
$ pip install .
